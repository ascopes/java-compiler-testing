name: Build
on:
  pull_request_target:
    branches: [ main ]
    types: [ opened, synchronize ]
  push:
    branches: [ main ]
  schedule:
    # Run a build once per week on the main branch.
    - cron: "0 0 * * 0"

jobs:
  codeql:
    name: Run CodeQL Analysis
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v2
        with:
          languages: java

      # This can be replaced with a manual build if this begins to fail for any reason.
      - name: Autobuild
        uses: github/codeql-action/autobuild@v2

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v2

  test:
    name: Build (Java ${{ matrix.java-version }} | ${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]
        java-version: [ 11, 12, 13, 14, 15, 16, 17, 18 ]
        include:
          - os: windows-latest
            build-script: './mvnw.cmd'
          - os: ubuntu-latest
            build-script: './mvnw'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          # Needed to stop some actions failing to read the commit SHA.
          fetch-depth: 2

      - name: Initialize JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '${{ matrix.java-version }}'

      - name: Compile and run tests
        run: >-
          ${{ matrix.build-script }} 
          -B 
          -U
          -T8C
          --no-transfer-progress
          '-Dcheckstyle.skip=true'
          '-Dstyle.color=always'
          '-Dmaven.artifact.threads=100'
          clean package

      - name: Annotate test reports
        if: always()
        run: >-
          scripts/prepare-test-outputs-for-merge.sh 
          ${{ matrix.java-version }} 
          ${{ matrix.os }}

      - name: Publish unit test results to pull requests
        uses: mikepenz/action-junit-report@v3
        if: always()
        with:
          check_name: JUnit - Java ${{ matrix.java-version }} - ${{ matrix.os }}
          report_paths: "**/TEST-*.xml"
          summary: JUnit results for Java ${{ matrix.java-version }} (${{ matrix.os }})
          update_check: true

      - name: Publish code coverage
        continue-on-error: true
        if: always()
        run: bash <(curl -s https://codecov.io/bash)

      - name: Archive Surefire reports
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: tests-java-${{ matrix.java-version }}-${{ matrix.os }}
          path: |
            **/target/surefire-reports/*.xml
            **/target/surefire-reports/*.txt
          retention-days: 5

      - name: Archive Jacoco reports
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: coverage-java-${{ matrix.java-version }}-${{ matrix.os }}
          path: "**/target/site/jacoco/jacoco*.xml"
          retention-days: 5

  checkstyle:
    name: Run Checkstyle
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Initialize Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Run Checkstyle
        run: >-
          ./mvnw 
          -B 
          -U 
          -T8C
          --no-transfer-progress
          -DskipTests=true
          -Dstyle.color=always
          -Dmaven.artifact.threads=100
          clean package

  javadoc:
    name: Generate JavaDocs
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Initialize Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          # We have to use 17 for now, as assertj causes JDK 14 and older to prevent linking
          # documentation correctly. I have opened an issue on GitHub with AssertJ to discuss
          # this being generated correctly: https://github.com/assertj/assertj-core/issues/2573.
          java-version: '17'

      - name: Generate JavaDoc documentation
        run: >-
          ./mvnw 
          -B 
          -U 
          -T8C
          --no-transfer-progress
          -Dmaven.test.skip=true
          -Dcheckstyle.skip=true
          -Dstyle.color=always
          -Dmaven.artifact.threads=100
          clean compile javadoc:jar

      - name: Archive JavaDoc artifacts
        uses: actions/upload-artifact@v2
        continue-on-error: true
        if: success()
        with:
          name: javadocs
          path: "**/target/apidocs/io.github.ascopes.jct"
          retention-days: 5
